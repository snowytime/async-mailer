{
	"name": "@snowytime/async-mailer",
	"version": "0.0.2",
	"description": "Simple extension of nodemailer to make it async-able, and with some other goodies",
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@swc/cli": "^0.1.59",
		"@types/nodemailer": "^6.4.7",
		"@typescript-eslint/eslint-plugin": "^5.48.0",
		"@typescript-eslint/parser": "^5.48.0",
		"dotenv": "^16.0.3",
		"eslint": "^8.31.0",
		"prettier": "^2.8.1",
		"react": "^18.2.0",
		"typescript": "^4.9.4"
	},
	"exports": "./dist/index.js",
	"scripts": {
		"build": "rm -rf dist && swc src -d dist && tsc -p ./tsconfig.build.json",
		"demo": "npm run build && ts-node -r dotenv/config demo/index.ts",
		"release:patch": "npm run build && git add . && git commit -m '🥳 auto patch' && git push origin master && npm version patch && npm publish"
	},
	"type": "module",
	"engines": {
		"node": ">=16.0.0"
	},
	"files": [
		"dist"
	],
	"types": "./dist/index.d.ts",
	"dependencies": {
		"nodemailer": "^6.8.0"
	}
}
